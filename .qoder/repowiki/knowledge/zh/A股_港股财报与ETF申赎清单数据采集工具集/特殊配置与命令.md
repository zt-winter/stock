每个脚本均可直接运行作为 CLI：`python financial_report.py` 启动多股票财报采集；`python stock_dividend.py` 采集分红明细；`python etf_redemption.py --fund-code 510300 --detail` 查看单只 ETF 成份股；`python etf_weight.py --fund-code 510300 --save` 计算权重并导出 CSV。数据库文件固定为同目录下的 `financial_data.db`，首次运行会自动建表。