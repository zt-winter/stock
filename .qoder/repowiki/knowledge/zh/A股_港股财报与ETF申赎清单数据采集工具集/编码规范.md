- 数据库连接统一通过 `get_conn()` 返回并启用 `PRAGMA journal_mode=WAL`，默认路径使用 `Path(__file__).parent / "financial_data.db"` 相对定位。
- 写入采用「先查表是否存在 → 按股票代码 DELETE 旧记录 → pandas to_sql append」的幂等更新模式，避免重复数据。
- 网络请求普遍采用 requests.Session + User-Agent Referer 头伪装浏览器，并对异常进行最多 3 次重试 + sleep 退避。
- 对 akshare 返回的日期列统一转为 datetime 后筛选近 10 年数据，并新增 `year`(int)、`quarter`(int) 两列作为统一时间维度。
- 命令行入口统一使用 argparse，支持 `--market sh|sz|all`、`--fund-code`、`--date`、`--save` 等参数，并在 `if __name__ == "__main__":` 中组织主流程。