__pycache__/
*.py[cod]

# 项目虚拟环境：各文档统一用 .venv/bin/python，但 venv 本身不入库
.venv/

# buffett-lens：语料原文与索引库不入库（版权 + 体积）
# 语料来自 berkshirehathaway.com 与 BRK-Annual-Meeting（无 LICENSE，要求注明 CNBC）
.claude/skills/buffett-lens/corpus/
.claude/skills/buffett-lens/buffett_corpus.db*

# 年报 PDF 原文不入库：单份可达 46MB（康师傅 2025），两家十份就 145MB。
# 抽出的分部数据已落在 financial_data.db 的 hk_segment_revenue，可随时用
# extract_segment_note.py 从原文重抽——原文留着是为了可复核，不必进 git。
report/*_*/

# 抽取/下载过程中的临时件
report/_tmp/

# 下载器生成的清单与校验报告：每次运行都会整体重写，入库只会持续产生噪音
report/download_log.md
report/validation_report.md

# download_financial_reports.py 在模块级 basicConfig 里挂了 FileHandler，
# 任何调用（含 --help）都会在 CWD 生成这个日志
financial_report_downloader.log

# SQLite WAL 副产物：每次开库都会变动，不忽略会持续污染 git status
*.db-wal
*.db-shm
